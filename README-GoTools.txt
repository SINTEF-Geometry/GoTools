Note to GoTools users:

This distribution of Newmat is included for convenience on Windows.
Use CMake/Visual Studio to install.

On Linux, you would probably use a package manager - apt-get, yum,
etc. - to install Newmat on the system.

jan.b.thomassen@sintef.no
